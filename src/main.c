#include <stdio.h>

#include <project.h>

int main() {
    file_t* file = new_file();
    dbuf_t* file_name = dbuf_create(1);
    dbuf_t* file_contents = dbuf_create(1);

    char* c_file_name = "test_file";
    char* c_file_contents = "beep boop beep";

    dbuf_append_sequence()

    return 0;
}
